<nav class="navbar navbar-expand-lg navbar-dark bg-primary position-fixed w-100" style="z-index: 100; top: 0;">
  <div class="container-fluid">
    <%= link_to "Project Manager", root_path, class: "navbar-brand" %>

    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
            aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav ms-auto">
        <% if user_signed_in? %>

          <li class="nav-item">
            <%= link_to (current_user.name.presence || current_user.email), edit_registration_path(current_user), class: "nav-link" %>
          </li>
            
          <% if current_user.manager? %>
            <li class="nav-item">
              <%= link_to "My Projects", projects_path, class: "nav-link" %>
            </li>
            
          <% elsif current_user.contributor? %>
            <li class="nav-item">
              <%= link_to "My Task", "#", class: "nav-link" %>
            </li>
          <% elsif current_user.admin? %>
            <li class="nav-item">
              <%= link_to "Projects", projects_path, class: "nav-link" %>
            </li>
            <li class="nav-item">
              <%= link_to "Users", admin_users_path, class: "nav-link" %>
            </li>
          <% end %>

          <li class="nav-item"> <%= button_to "Logout", destroy_user_session_path, method: :delete, class: "nav-link btn btn-link btn-outline-danger text-light mx-2" %>

        <% else %>
          <li class="nav-item"> <%= link_to "Login/Signup", new_user_session_path, class: "nav-link btn btn-outline-secondary text-light mx-2" %> </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
